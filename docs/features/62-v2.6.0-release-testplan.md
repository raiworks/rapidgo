# 🧪 Test Plan: v2.6.0 Release

> **Feature**: `62` — v2.6.0 Release
> **Architecture**: [`architecture.md`](62-v2.6.0-release-architecture.md)

---

## ENH-06: Logger Interface

| ID | Test | Input | Expected | File |
|----|------|-------|----------|------|
| TC-L01 | `SlogLogger` implements `Logger` interface | Compile-time check via `var _ Logger = (*SlogLogger)(nil)` | Compiles | `logger_test.go` |
| TC-L02 | `With()` returns new `Logger` with attrs | `NewSlogLogger(l).With("key", "val")` | Returns `Logger`, logs include `key=val` | `logger_test.go` |
| TC-L03 | `Setup()` returns `Logger` (not `*slog.Logger`) | `logger.Setup()` | Type is `Logger` interface | `logger_test.go` |
| TC-L04 | `SlogLogger` methods log to underlying handler | `Debug/Info/Warn/Error` on `SlogLogger` wrapping buffer handler | Messages appear in buffer | `logger_test.go` |

---

## ENH-05: LoadConfig[T]()

| ID | Test | Input | Expected | File |
|----|------|-------|----------|------|
| TC-C01 | All basic types parsed | Env vars for string/int/bool/float64 fields | Struct populated correctly | `config_test.go` |
| TC-C02 | Default values used when env empty | No env vars set, struct has `default:` tags | Defaults applied | `config_test.go` |
| TC-C03 | Validation failure | Field with `validate:"required"`, env var absent, no default | Error returned | `config_test.go` |
| TC-C04 | Required field present | Field with `validate:"required"`, env var set | No error | `config_test.go` |
| TC-C05 | Duration parsing | `TIMEOUT=30s` | `time.Duration` field = 30s | `config_test.go` |
| TC-C06 | Slice string comma split | `HOSTS=a,b,c` | `[]string{"a", "b", "c"}` | `config_test.go` |
| TC-C07 | Unknown type returns error | Struct with unsupported field type (e.g., `complex64`) | Error | `config_test.go` |
| TC-C08 | Fields without `env` tag skipped | Field without tag, env var set | Field stays zero | `config_test.go` |

---

## ENH-02a: GenerateTokenFromString()

| ID | Test | Input | Expected | File |
|----|------|-------|----------|------|
| TC-J01 | String token generation | `GenerateTokenFromString("uuid-123")` | Non-empty JWT string | `auth_test.go` |
| TC-J02 | String token validation | Generate → Validate | `claims["user_id"]` is `string`, value = `"uuid-123"` | `auth_test.go` |
| TC-J03 | Fails without secret | `JWT_SECRET=""` | Error: "JWT_SECRET is not set" | `auth_test.go` |
| TC-J04 | Existing uint tests pass | All TC-01–TC-08 | All pass (refactored to use `jwtConfig()`) | `auth_test.go` |

---

## Regression

| ID | Test | Expected |
|----|------|----------|
| REG-01 | `go test -count=1 ./...` | All packages pass (currently 35) |
| REG-02 | `go build ./...` | Clean build, no errors |
