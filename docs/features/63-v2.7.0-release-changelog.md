# 📋 Changelog: v2.7.0 Release

> **Feature**: `63` — v2.7.0 Release
> **Tasks**: [`tasks.md`](63-v2.7.0-release-tasks.md)

---

### Session 1 — Build (2026-03-15)

**Phase 1 — ENH-02: UUIDBaseModel**
- Added `UUIDBaseModel` struct to `database/models/base.go` with `ID string`, `CreatedAt`, `UpdatedAt`, `DeletedAt`
- Added `BeforeCreate` GORM hook that generates UUID v4 via `uuid.NewString()` if ID is empty
- Added 5 tests: TC-U01 (fields), TC-U02 (embed), TC-U03 (AutoMigrate), TC-U04 (auto-generate UUID), TC-U05 (preserve pre-set ID)

**Phase 2 — ENH-07: make:module**
- Added `makeModuleCmd` to `core/cli/make_scaffold.go` with 4 templates: models.go, service.go, controller.go, routes.go
- Module scaffolded into `modules/<snake_name>/` directory
- Registered command in `root.go` (now 20 total commands)
- Added 1 test: TC-M01 (creates 4 files in correct directory)

**Phase 3 — Finalize**
- Version → `"2.7.0"`, all 35 packages pass
