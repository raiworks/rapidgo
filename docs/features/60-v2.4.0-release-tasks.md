# 📋 Tasks: v2.4.0 Release

> **Feature**: `60` — v2.4.0 Release
> **Architecture**: [`60-v2.4.0-release-architecture.md`](60-v2.4.0-release-architecture.md)
> **Date**: 2026-03-15

---

## Phase 1 — IMP-02: Consistent `config.Load()` (Do First)

- [x] 1.1 Add `config.Load()` call at the top of `NewApp()` in `core/cli/root.go`
- [x] 1.2 Keep the early `config.Load()` in `serve` command's `RunE` (needed before `NewApp()` for `RAPIDGO_MODE`) — the duplicate call in `NewApp()` is harmless (idempotent)
- [x] 1.3 Remove `config.Load()` from `work` command's `RunE` in `core/cli/work.go` (now handled by `NewApp()`)
- [x] 1.4 Remove `config.Load()` from `schedule:run` command's `RunE` in `core/cli/schedule_run.go` (now handled by `NewApp()`)

### Checkpoint 1
- [x] `serve` still reads `RAPIDGO_MODE` correctly
- [x] `migrate`, `db:seed` now read `.env` values (DB_HOST, etc.)
- [x] All existing tests pass

---

## Phase 2 — BUG-02: Fix `serveSingle()` Route Registration

- [x] 2.1 Add `applyRoutesForMode(r, application.Container, mode)` call in `serveSingle()` after getting the router from the container
- [x] 2.2 Verify `routeRegistrar` fires for single-port serve

### Checkpoint 2
- [x] `SetRoutes()` callback fires in single-port mode
- [x] Health routes register in single-port mode
- [x] Template/static loading happens in single-port mode

---

## Phase 3 — BUG-03: Fix `serveMulti()` Provider Middleware

- [x] 3.1 Add `GlobalHandlers()` method to `Router` in `core/router/router.go`
- [x] 3.2 Update `serveMulti()` to get the container's router
- [x] 3.3 Copy global handlers from container router to each per-service router before calling `applyRoutesForMode()`

### Checkpoint 3
- [x] Provider-registered global middleware applies to all per-service routers
- [x] Per-service routers still get correct mode-specific routes
- [x] Multi-port mode works with separate ports

---

## Phase 4 — IMP-01: Configurable Server Timeouts

- [x] 4.1 Add `parseDuration()` helper in `core/cli/serve.go`
- [x] 4.2 Add `resolveServerTimeouts()` helper returning read, write, idle, shutdown durations
- [x] 4.3 Update `serveSingle()` to use `resolveServerTimeouts()`
- [x] 4.4 Update `serveMulti()` to use `resolveServerTimeouts()`
- [x] 4.5 Add `parseDuration` unit test in `core/cli/serve_test.go`

### Checkpoint 4
- [x] Default behavior unchanged (same values as current hardcoded)
- [x] Setting `SERVER_READ_TIMEOUT=30s` changes read timeout
- [x] Invalid duration strings fall back to defaults

---

## Phase 5 — FEAT-04: Router `NoRoute()`

- [x] 5.1 Add `NoRoute(handlers ...gin.HandlerFunc)` method to `Router` in `core/router/router.go`
- [x] 5.2 Add unit test in `core/router/router_test.go`

### Checkpoint 5
- [x] `r.NoRoute(handler)` works without accessing `r.Engine()` directly

---

## Phase 6 — FEAT-03: Health Endpoint with Version

- [x] 6.1 Update `health.Routes()` signature to accept optional `version ...string`
- [x] 6.2 Include version in `/health` response when provided
- [x] 6.3 Update call site in `applyRoutesForMode()` to pass `cli.Version`
- [x] 6.4 Add unit test for health with and without version

### Checkpoint 6
- [x] `GET /health` with version returns `{"status":"ok","version":"2.4.0"}`
- [x] `GET /health` without version returns `{"status":"ok"}` (backward compat)
- [x] `GET /health/ready` unchanged

---

## Phase 7 — Final

- [x] 7.1 Run full test suite
- [x] 7.2 Update `core/cli/root.go` Version constant to `"2.4.0"`
- [x] 7.3 Update changelog
- [x] 7.4 Commit and tag `v2.4.0`
