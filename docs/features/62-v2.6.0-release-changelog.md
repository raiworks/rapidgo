# 📋 Changelog: v2.6.0 Release

> **Feature**: `62` — v2.6.0 Release
> **Tasks**: [`tasks.md`](62-v2.6.0-release-tasks.md)

---

### Session 1 — Build (2026-03-15)

**Phase 1 — ENH-06: Logger Interface**
- Added `Logger` interface to `core/logger/logger.go` (5 methods: `Debug`, `Info`, `Warn`, `Error`, `With`)
- Added `SlogLogger` struct + `NewSlogLogger()` constructor implementing `Logger`
- Changed `Setup()` return type from `*slog.Logger` to `Logger`
- All 14 global `slog.*` call sites unchanged (still work via `slog.SetDefault`)
- Added 4 tests: TC-L01 (compile-time interface check), TC-L02 (With attrs), TC-L03 (Setup returns Logger), TC-L04 (all methods log)

**Phase 2 — ENH-05: LoadConfig[T]()**
- Added `LoadConfig[T any]() (T, error)` to `core/config/config.go`
- Added `setField()` helper supporting: string, int, int64, bool, float64, time.Duration, []string
- Struct tags: `env:"KEY"`, `default:"value"`, `validate:"rule"`
- Validation via `go-playground/validator` (indirect → direct dep)
- Added 8 tests: TC-C01–TC-C08 (all types, defaults, validation, duration, slices, unsupported, no-tag)

**Phase 3 — ENH-02a: GenerateTokenFromString()**
- Extracted `jwtConfig()` helper from `GenerateToken()` (DRY)
- Refactored `GenerateToken(uint)` to use `jwtConfig()`
- Added `GenerateTokenFromString(userID string)` for UUID support
- Updated `ValidateToken` doc comment re: user_id claim types
- Added 3 tests: TC-J01 (string token), TC-J02 (string in claims), TC-J03 (fails without secret)
- All 10 existing auth tests pass after refactor (TC-J04 verified)

**Phase 4 — Finalize**
- Version → `"2.6.0"`, all 35 packages pass

### Ship — 2026-03-15

- Cross-checked all source files against architecture doc — found 1 discrepancy: `go.mod` validator still `// indirect`. Fixed with `go mod tidy`.
- All 35 packages pass (uncached `-count=1` run), clean build
- Merged `feature/62-v2.6.0-release` → `main` (no-ff)
- Tagged `v2.6.0`, pushed to origin
- Updated project-roadmap.md: feature #62 → ✅
- Updated index.md: v2.6.0 → ✅ Shipped, ENH-06/ENH-05/ENH-02a → SHIPPED, MOD-01 → DEFERRED
- Updated tasks.md: all checkboxes checked
