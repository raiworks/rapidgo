# 📋 Tasks: v2.5.0 Release

> **Feature**: `61` — v2.5.0 Release
> **Architecture**: [`61-v2.5.0-release-architecture.md`](61-v2.5.0-release-architecture.md)
> **Date**: 2026-03-15

---

## Phase 1 — ENH-08: Expanded Error Codes

- [x] 1.1 Rename `AppError.Code int` → `AppError.Status int` in `core/errors/errors.go`
- [x] 1.2 Add `Code string` field to `AppError`
- [x] 1.3 Add `WithCode(code string) *AppError` builder method
- [x] 1.4 Add `HTTPStatus() int` deprecated helper method
- [x] 1.5 Update all 7 factory functions (`NotFound`, `BadRequest`, `Internal`, `Unauthorized`, `Forbidden`, `Conflict`, `Unprocessable`) to set both `Status` and default `Code`
- [x] 1.6 Update `ErrorResponse()` to include `code` in response
- [x] 1.7 Update error handler middleware (`core/middleware/error_handler.go` lines 22, 27) to use `Status` instead of `Code`
- [x] 1.8 Update `core/errors/errors_test.go` references (`appErr.Code` → `appErr.Status` at lines 49-50, 82-83)
- [x] 1.9 Search and update any other internal framework references to `AppError.Code` (now `Status`)
- [x] 1.10 Add/update tests in `core/errors/errors_test.go` for new `Code string` field and `WithCode()`

### Checkpoint 1
- [x] All factories return correct `Status` and `Code`
- [x] `WithCode()` overrides default code
- [x] `ErrorResponse()` includes `"code"` field
- [x] All internal references compile

---

## Phase 2 — ENH-09: Rate Limit Convenience Helpers

- [x] 2.1 Add `KeyByIP()` function in `core/middleware/ratelimit.go`
- [x] 2.2 Add `KeyByUserID(contextKey string)` function
- [x] 2.3 Add `KeyByHeader(header string)` function
- [x] 2.4 Add `KeyByBodyField(field string)` function
- [x] 2.5 Add `ParseRate(rate string)` function
- [x] 2.6 Add tests for all helpers in `core/middleware/ratelimit_test.go`

### Checkpoint 2
- [x] All key functions return correct keys
- [x] Fallback to IP when key source is missing
- [x] `ParseRate()` validates format correctly

---

## Phase 3 — IMP-06: `container.TryMake()`

- [x] 3.1 Add `TryMake(name string) (interface{}, error)` to `Container`
- [x] 3.2 Add generic `TryMake[T any](c *Container, name string) (T, error)` function
- [x] 3.3 Add tests in `core/container/container_test.go`

### Checkpoint 3
- [x] `TryMake` returns error for missing service (no panic)
- [x] `TryMake` returns value for registered service
- [x] Generic `TryMake[T]` catches type assertion failures

---

## Phase 4 — FEAT-01: Local Mode Helper

- [x] 4.1 Add `IsLocal() bool` to `core/config/environment.go` (alongside `IsProduction()`, `IsDevelopment()`, `IsTesting()`)
- [x] 4.2 Add test for `IsLocal()` in `core/config/environment_test.go`

### Checkpoint 4
- [x] `IsLocal()` returns true for "local" and "development"
- [x] `IsLocal()` returns false for "production", "testing"

---

## Phase 5 — IMP-05: `migrate:fresh` Command

- [x] 5.1 Create `core/cli/migrate_fresh.go`
- [x] 5.2 Implement table drop + re-migration logic
- [x] 5.3 Add `--force` flag requirement for production
- [x] 5.4 Register command in `root.go`
- [x] 5.5 Add test for production guard

### Checkpoint 5
- [x] `migrate:fresh` drops and recreates all tables
- [x] Refuses to run in production without `--force`

---

## Phase 6 — FEAT-02: `db:wipe` Command

- [x] 6.1 Create `core/cli/db_wipe.go`
- [x] 6.2 Implement table truncation with dialect awareness (PostgreSQL CASCADE, MySQL, SQLite)
- [x] 6.3 Skip `migrations` tracking table
- [x] 6.4 Add `--force` flag requirement for production
- [x] 6.5 Register command in `root.go`
- [x] 6.6 Add test for production guard

### Checkpoint 6
- [x] `db:wipe` truncates all user tables
- [x] Preserves `migrations` table
- [x] Refuses to run in production without `--force`

---

## Phase 7 — IMP-03 + IMP-04: Seeder Improvements

- [x] 7.1 Improve nil seeder error message in `core/cli/seed.go`
- [x] 7.2 Add `SeederListFunc` type and `SetSeederList()` to `core/cli/hooks.go`
- [x] 7.3 Add `--list` flag to `db:seed` command
- [x] 7.4 Implement list output when `--list` is passed

### Checkpoint 7
- [x] `db:seed --list` shows registered seeders
- [x] Clear error when no seeder function registered

---

## Phase 8 — FEAT-05: `make:seeder` Scaffold

- [x] 8.1 Add `seederTpl` template to `core/cli/make_scaffold.go`
- [x] 8.2 Add `makeSeederCmd` command
- [x] 8.3 Register command in `root.go`
- [x] 8.4 Add test that scaffold generates correct file

### Checkpoint 8
- [x] `make:seeder UserSeeder` creates `database/seeders/user_seeder.go`
- [x] Generated file compiles

---

## Phase 9 — FEAT-06: Configurable Banner

- [x] 9.1 Extract banner printing to `printBanner()` function in `core/cli/serve.go`
- [x] 9.2 Support `APP_BANNER` env var override
- [x] 9.3 Update both single-port and multi-port banner printing to use `printBanner()`

### Checkpoint 9
- [x] Default banner unchanged
- [x] Setting `APP_BANNER` replaces default output

---

## Phase 10 — Final

- [x] 10.1 Run full test suite
- [x] 10.2 Update `core/cli/root.go` Version constant to `"2.5.0"`
- [x] 10.3 Update changelog
- [x] 10.4 Commit and tag `v2.5.0`
