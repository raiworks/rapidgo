# ✅ Tasks: v2.6.0 Release

> **Feature**: `62` — v2.6.0 Release
> **Architecture**: [`architecture.md`](62-v2.6.0-release-architecture.md)
> **Test Plan**: [`testplan.md`](62-v2.6.0-release-testplan.md)
> **Changelog**: [`changelog.md`](62-v2.6.0-release-changelog.md)

---

## Phase 1 — ENH-06: Logger Interface

- [ ] 1.1 — Add `Logger` interface to `core/logger/logger.go` (5 methods: `Debug`, `Info`, `Warn`, `Error`, `With`)
- [ ] 1.2 — Add `SlogLogger` struct implementing `Logger` with `NewSlogLogger()` constructor
- [ ] 1.3 — Change `Setup()` return type from `*slog.Logger` to `Logger`
- [ ] 1.4 — Add tests: TC-L01 (SlogLogger implements Logger), TC-L02 (With returns new Logger), TC-L03 (Setup returns Logger interface), TC-L04 (SlogLogger logs to buffer)

### Checkpoint 1
- [ ] `go build ./core/logger/...` passes
- [ ] `go test ./core/logger/...` passes
- [ ] `go test ./...` — all packages still pass (no breakage from type change)

---

## Phase 2 — ENH-05: Typed Config LoadConfig[T]()

- [ ] 2.1 — Add `setField()` unexported helper in `core/config/config.go` (handles string, int, int64, bool, float64, time.Duration, []string)
- [ ] 2.2 — Add `LoadConfig[T any]() (T, error)` generic function with `env:`, `default:`, and `validate:` struct tag support
- [ ] 2.3 — Add validator direct import, update `go.mod`
- [ ] 2.4 — Add tests: TC-C01 (all field types), TC-C02 (defaults used when env empty), TC-C03 (validation fail), TC-C04 (required fields), TC-C05 (duration parsing), TC-C06 ([]string comma split)

### Checkpoint 2
- [ ] `go build ./core/config/...` passes
- [ ] `go test ./core/config/...` passes
- [ ] `go test ./...` — all packages pass

---

## Phase 3 — ENH-02a: GenerateTokenFromString()

- [ ] 3.1 — Extract `jwtConfig()` unexported helper from `GenerateToken()` logic
- [ ] 3.2 — Refactor `GenerateToken(uint)` to use `jwtConfig()`
- [ ] 3.3 — Add `GenerateTokenFromString(string)` using `jwtConfig()`
- [ ] 3.4 — Add tests: TC-J01 (string token generation), TC-J02 (string token validation — user_id is string in claims), TC-J03 (GenerateTokenFromString fails without secret), TC-J04 (existing uint tests still pass after refactor)

### Checkpoint 3
- [ ] `go build ./core/auth/...` passes
- [ ] `go test ./core/auth/...` passes
- [ ] `go test ./...` — all packages pass

---

## Phase 4 — Finalize

- [ ] 4.1 — Bump version to "2.6.0" in `core/cli/root.go`
- [ ] 4.2 — Run full test suite: `go test -count=1 ./...`
- [ ] 4.3 — Run `go build ./...`
- [ ] 4.4 — Update changelog.md with build session details
- [ ] 4.5 — Commit: `feat(v2.6.0): Logger interface, LoadConfig[T], GenerateTokenFromString`
