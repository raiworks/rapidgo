# 🧪 Test Plan: v2.1.0 Release

> **Feature**: `59` — v2.1.0 Release
> **Date**: 2026-03-11

---

## Test Strategy

Each code change has unit tests in its package. Integration-style tests use SQLite in-memory for database tests.

---

## Test Cases

### GORM Query Logging (I-005)

| # | Test | Expected |
|---|---|---|
| T-001 | Connect with `APP_ENV=development` | GORM logger configured at Info level |
| T-002 | Connect with `APP_ENV=production` | GORM logger is silent (default) |
| T-003 | Connect with `DB_LOG=true` in production | Logger activates regardless of env |

### WebSocket Heartbeat (I-001)

| # | Test | Expected |
|---|---|---|
| T-004 | `NewHub()` uses default config (30s/10s) | PingInterval=30s, PongTimeout=10s |
| T-005 | `NewHubWithConfig()` with custom values | Config values applied |
| T-006 | OnJoin callback fires on Join | Callback called with correct client and room |
| T-007 | OnLeave callback fires on Leave | Callback called with correct client and room |
| T-008 | OnLeave callback fires on Remove | Fires for each room the client was in |

### Queue Backoff (I-002)

| # | Test | Expected |
|---|---|---|
| T-009 | `DispatchWithBackoff()` sets BackoffSeconds on job | Job.BackoffSeconds matches input |
| T-010 | Worker retry with backoff uses correct delay | 1st fail=10s, 2nd=30s, 3rd=120s |
| T-011 | Worker retry without backoff uses flat RetryDelay | Backward compatible |
| T-012 | Backoff array shorter than attempts uses last value | Clamps to final element |

### Rate Limiter (I-003)

| # | Test | Expected |
|---|---|---|
| T-013 | `RateLimitMiddleware()` still keys by IP | Backward compatible |
| T-014 | `RateLimitWithConfig()` with custom KeyFunc | Uses provided key |
| T-015 | `RateLimitWithConfig()` with custom rate | Respects rate string |

### Pagination (F-002)

| # | Test | Expected |
|---|---|---|
| T-016 | `Paginate()` page=1, perPage=10 | Correct offset=0, results, total |
| T-017 | `Paginate()` page > totalPages | Empty results, correct metadata |
| T-018 | `Paginate()` perPage clamped to 1–100 | perPage=0→15, perPage=200→100 |
| T-019 | `CursorPaginate()` first page (no cursor) | Returns results, next_cursor set, has_more=true |
| T-020 | `CursorPaginate()` with cursor | Returns results after cursor |
| T-021 | `CursorPaginate()` last page | has_more=false, no next_cursor |
| T-022 | `CursorPaginate()` prev direction | Returns results before cursor |
| T-023 | `CursorPaginate()` empty table | Empty results, has_more=false |

### Notification System (F-004)

| # | Test | Expected |
|---|---|---|
| T-024 | Send notification to database channel | NotificationRecord created with correct data |
| T-025 | Send notification to mail channel | Mail.Send() called with correct subject/body |
| T-026 | Multi-channel notification | Both database and mail channels fire |
| T-027 | Unknown channel in Channels() | Error returned, no panic |
| T-028 | Notification without database interface | Skips database channel gracefully |

---

## Acceptance Criteria

1. All existing tests pass (no regressions)
2. All new tests pass
3. `go vet ./...` clean
4. `go build ./...` clean
5. Existing API signatures unchanged (backward compatible)
