# Feature #39 — Docker Deployment: Plan

## Tasks

1. Create `Dockerfile` (multi-stage).
2. Create `docker-compose.yml` (app + postgres + redis).
3. Create `.dockerignore`.
4. Commit, merge to main, push.

## Test Plan

No automated tests — Docker config validated by `docker compose config`.
