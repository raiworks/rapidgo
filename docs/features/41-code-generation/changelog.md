# Feature #41 — Code Generation (CLI): Changelog

## [Unreleased]

### Added
- `make:controller` — scaffolds controller with CRUD methods.
- `make:model` — scaffolds model with BaseModel embed.
- `make:service` — scaffolds service with DB field and constructor.
- `make:provider` — scaffolds service provider with Register/Boot stubs.
- Shared `scaffold()` helper for all generators.
