# Feature #38 — Caddy Integration: Plan

## Tasks

1. Create `Caddyfile` in project root with reverse proxy config.
2. Write documentation.
3. Commit, merge to main, push.

## Test Plan

No automated tests — this is a configuration file. Validation is manual (Caddy --validate).
