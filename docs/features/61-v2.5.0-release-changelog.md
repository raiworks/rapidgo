# 📝 Changelog: v2.5.0 Release

> **Feature**: `61` — v2.5.0 Release
> **Date Started**: 2026-03-15

---

## Changes

### Session 1 — 2026-03-15

**Phase 1 — ENH-08: Expanded Error Codes**
- Renamed `AppError.Code int` → `AppError.Status int` in `core/errors/errors.go`
- Added `Code string` field for machine-readable error codes
- Added `WithCode(code string)` builder and `HTTPStatus()` deprecated helper
- Updated all 7 factory functions to set both `Status` and default `Code`
- Updated `ErrorResponse()` to include `"code"` field
- Updated error handler middleware — `.Code` → `.Status`
- Added 3 new tests: TC-15 WithCode, TC-16 ErrorResponse code, TC-17 HTTPStatus

**Phase 2 — ENH-09: Rate Limit Helpers**
- Added `KeyByIP()`, `KeyByUserID()`, `KeyByHeader()`, `ParseRate()`
- Created `ratelimit_test.go` with 7 tests (T-011–T-017)

**Phase 3 — IMP-06: `container.TryMake()`**
- Added `TryMake()` and generic `TryMake[T]()` to container
- Added 4 tests (TC-18–TC-21)

**Phase 4 — FEAT-01: `IsLocal()` Helper**
- Added `IsLocal()` to `core/config/environment.go`
- Created `environment_test.go` with 4 tests (T-022–T-025)

**Phase 5 — IMP-05: `migrate:fresh` Command**
- Created `core/cli/migrate_fresh.go` with production guard

**Phase 6 — FEAT-02: `db:wipe` Command**
- Created `core/cli/db_wipe.go` — dialect-aware truncation, skips `migrations` table

**Phase 7 — IMP-03+04: Seeder Improvements**
- Added `SeederListFunc` + `SetSeederList()` hook, `--list` flag on `db:seed`

**Phase 8 — FEAT-05: `make:seeder` Scaffold**
- Added `makeSeederCmd` + `seederTpl` to `make_scaffold.go`

**Phase 9 — FEAT-06: Configurable Banner**
- Extracted `printBanner()` with `APP_BANNER` env var support

**Phase 10 — Finalize**
- Version → `"2.5.0"`, 3 new commands registered, all 35 packages pass

### Ship — 2026-03-15

- Cross-checked all source files against architecture doc — found `KeyByBodyField()` was missing (task 2.4). Fixed with safe body re-buffering (`io.ReadAll` + `io.NopCloser`)
- All 35 packages pass (uncached `-count=1` run), clean build
- Merged `feature/61-v2.5.0-release` → `main` (no-ff)
- Tagged `v2.5.0`, pushed to origin
- Updated project-roadmap.md: feature #61 → ✅
- Updated index.md: v2.5.0 → ✅ Shipped, all 10 items → SHIPPED
- Updated tasks.md: all checkboxes checked
